# ============================================================
# 糕点产线工业级智慧质检系统 - Python依赖清单
# 说明：
#   - 基础依赖为所有平台必装
#   - ultralytics 为 YOLO 模型推理可选依赖（规则法模式无需安装）
#   - rknnlite2 仅在 RK3588/RK3572 硬件上需要，需从瑞芯微官方源安装
# ============================================================

# --- 基础依赖（必装）---
Flask>=2.3.0,<4.0.0          # Web 服务框架
opencv-python>=4.8.0         # 计算机视觉处理（摄像头采集+图像处理）
numpy>=1.24.0                # 数值计算
PyYAML>=6.0                  # YAML 配置文件解析
Pillow>=10.0.0               # 图像处理辅助

# --- YOLO 模型推理（可选，使用规则法时可跳过）---
# 安装命令：pip install ultralytics
# ultralytics>=8.1.0         # YOLOv8 推理与训练（笔记本/工控机用）

# --- RK3588/RK3572 NPU 推理（仅瑞芯微硬件需要，不可在 x86 上安装）---
# 安装命令（在 RK3588 上执行）：
#   pip install rknnlite2 --index-url https://pypi.rock-chips.com/simple/
# rknnlite2>=1.6.0           # RKNN 运行时（RK3588 NPU 推理用）
